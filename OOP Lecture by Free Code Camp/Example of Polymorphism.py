s = "something"
print(len(s))

l = ["some", "thing"]
print(len(l))

'''
That's Polymorphism ... a single function works in a different way for different functions.
len() gives the number of characters if a str is passed but number of elements if a list/array is passed. 
'''
